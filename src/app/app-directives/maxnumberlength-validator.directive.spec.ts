import { MaxnumberlengthValidatorDirective } from './maxnumberlength-validator.directive';

describe('MaxnumberlengthValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxnumberlengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
